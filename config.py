#Settings for web server
log_events = True # Logs events to logs.txt
upload_folder = 'uploads' # uploads is the default folder
port = '5000' # 5000 is the default
host = '0.0.0.0' # 0.0.0.0 is the default
run_python_files = False #Unstable and unsafe, it will run a Python script if sent to the server. False is the default
print(" * Settings loaded. See config.py for settings")
